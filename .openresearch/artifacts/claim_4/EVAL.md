# Claim 4 evaluation

Verdict: **FALSIFIED** for Theorem 1.6 as written.

Reason: the theorem allows `p_n=n^-2`, for which it demands
`Theta(1/p_n)=Theta(n^2)` high-variance variables among only `n` variables.
The exact rational checker verifies the obstruction. A fixed-`p` negative
control is rejected, demonstrating that the verifier is sensitive to the
missing asymptotic condition.

The live judge has not evaluated this evidence. No score increase is claimed.

