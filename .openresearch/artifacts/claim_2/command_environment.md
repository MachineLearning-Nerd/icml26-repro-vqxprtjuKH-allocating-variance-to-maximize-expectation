# Command and environment — Claim 2

Fixed cumulative command:
`uv run --frozen python -m reproduction.run_all`.

The certificate is deterministic, single-worker CPU algebra. It uses the same
repository `.venv`, Python 3.12, `pyproject.toml`, and `uv.lock` as every prior
node.
